# Testing
Run `npm test` for unit tests and `npm run test:watch` during development. The suite focuses on engine state transitions, terminal safety, progress math and streak calculations. Add a regression test whenever a command or state transition changes.
