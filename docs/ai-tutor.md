# AI Tutor
`/api/ai` calls Groq only on the server. The API key is never shipped to the client. The tutor prompt scopes responses to Git and version-control education and accepts optional lesson/terminal context.
