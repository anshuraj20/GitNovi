# Git Engine
The simulator models a working tree, index, object database, commits, refs, HEAD, reflogs, remotes, stash and selected merge/recovery behavior. Object IDs are deterministic SHA-1 hashes of simulated content plus commit metadata. The implementation is educational rather than a byte-for-byte reimplementation of Git.
