'use client'; import {useState} from 'react';
export function ProfileForm({initialName,email}:{initialName:string;email:string}){const [name,setName]=useState(initialName);const [msg,setMsg]=useState('');const [loading,setLoading]=useState(false);const save=async(e:React.FormEvent)=>{e.preventDefault();setLoading(true);const r=await fetch('/api/profile',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({displayName:name})});setMsg(r.ok?'Profile saved.':((await r.json()).error??'Unable to save profile.'));setLoading(false)};return <form onSubmit={save} className="card p-6 mt-8 space-y-5"><label className="block text-sm"><span className="text-slate-300">Email</span><input value={email} readOnly className="mt-2 w-full card bg-transparent px-4 py-3 opacity-70" /></label><label className="block text-sm"><span className="text-slate-300">Display name</span><input required value={name} onChange={e=>setName(e.target.value)} className="mt-2 w-full card bg-transparent px-4 py-3" /></label><button disabled={loading} className="px-5 py-2.5 rounded-lg bg-cyan-500 text-slate-950 font-bold">{loading?'Saving…':'Save profile'}</button>{msg&&<p className="text-sm text-slate-300" role="status">{msg}</p>}</form>}
